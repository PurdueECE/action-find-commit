name: Integration Tests
on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./
        with:
          token: ${{ secrets.GEOFF_PAT }}
          repository: PurdueECE/action-find-commit
          before: '2023/01/01 23:59:59-05:00'